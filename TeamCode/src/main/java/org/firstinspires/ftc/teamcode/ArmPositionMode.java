package org.firstinspires.ftc.teamcode;

/*
 * This is an enum to represent the different position modes that the arm goes between. Enums are
 * like booleans that can be set to a word rather than true/false.
 */
public enum ArmPositionMode {
    COMPACT,
    COLLECT,
    PLACE,
}
